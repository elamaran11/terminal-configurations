# This file is too large for inline content.
# The actual .p10k.zsh config has been uploaded via the backup-current.sh script.
# Run: cp ~/.p10k.zsh p10k/.p10k.zsh && git add -A && git commit -m "update p10k" && git push
